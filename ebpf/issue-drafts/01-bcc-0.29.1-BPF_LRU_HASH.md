# タイトル案
Ubuntu 24.04 の BCC 0.29.1 で `BPF_LRU_HASH` がコンパイルエラーになる

# 本文

README の Ubuntu 向け手順（`apt install bpfcc-tools python3-bpfcc linux-headers-$(uname -r)`）で BCC を入れて実行すると、
BPF プログラムのコンパイルで失敗します。

## 環境

- Ubuntu 24.04.4 LTS、kernel 6.8.0-117-generic
- BCC: `bpfcc-tools` / `python3-bpfcc` 0.29.1+ds-1ubuntu7（Ubuntu 標準パッケージ）
- blocking-io-check 0.2.0（main、コミット 0f53fc2 時点）
- x86_64 と arm64（aarch64）の両方で同じエラー

## 再現手順

```bash
git clone https://github.com/ryuichi1208/blocking-io-check-py && cd blocking-io-check-py
uv venv --system-site-packages && uv sync
sudo .venv/bin/blocking-io-check -p nomatch -d 2
```

## 実際の出力

```
/virtual/main.c:106:27: error: expected identifier
  106 | BPF_LRU_HASH(fd_nonblock, struct key_t, u8, 65536);
      |                           ^
/virtual/main.c:106:1: error: type specifier missing, defaults to 'int'; ISO C99 and later do not support implicit int [-Wimplicit-int]
/virtual/main.c:107:27: error: expected identifier
  107 | BPF_LRU_HASH(fd_epoll,    struct key_t, u8, 65536);
/virtual/main.c:108:27: error: expected identifier
  108 | BPF_LRU_HASH(peer_map,    struct key_t, struct peer_t, 65536);
...
/virtual/main.c:157:9: error: use of undeclared identifier 'fd_nonblock'
Exception: Failed to compile BPF module <text>
```

## 切り分け

同じ BCC で最小のプログラムを試したところ、`BPF_LRU_HASH` は引数の数（3 個 / 4 個）や構造体名に関係なく落ち、
`BPF_HASH` の 4 引数や、`BPF_TABLE("lru_hash", ...)` の明示形は通りました。

| 書き方 | BCC 0.29.1 での結果 |
|---|---|
| `BPF_LRU_HASH(m, struct key_t, u8, 1024)` | 失敗 |
| `BPF_LRU_HASH(m, struct key_t, u8)` | 失敗 |
| `BPF_HASH(m, struct key_t, u8, 1024)` | 成功 |
| `BPF_TABLE("lru_hash", struct key_t, u8, m, 65536)` | 成功 |

## 回避策

`trace.bpf.c` の 3 行を明示形に書き換えると、それ以降のコンパイルは通りました。

```c
BPF_TABLE("lru_hash", struct key_t, u8, fd_nonblock, 65536);
BPF_TABLE("lru_hash", struct key_t, u8, fd_epoll, 65536);
BPF_TABLE("lru_hash", struct key_t, struct peer_t, peer_map, 65536);
```

BCC の内部（マクロ側）の事情は理解できていないので、原因の断定は避けます。より新しい BCC では問題ないのかもしれません。
README の Ubuntu 手順で入る版がこれなので、対応いただけると Ubuntu ユーザーは助かると思います。

## 参考

再現に使った環境一式とパッチ: https://github.com/HarukaYX/fastapi-blocking-io-lab/tree/main/ebpf
